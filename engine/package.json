{
  "name": "kartpilot-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "KartPilot faizsiz gün hesaplama motoru — saf TypeScript, UI bağımsız. Kaynak gerçek (source of truth) bu pakettir; Lovable projesindeki kopya buradan güncellenir.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
